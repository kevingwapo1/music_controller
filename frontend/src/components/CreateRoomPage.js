import React, { Component } from "react";

// Define the CreateRoomPage component
export default class CreateRoomPage extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <h1>Create a New Room</h1>; // Return content, not the component itself
  }
}
